package test;

public class Demo2 {
    public static void main(String[] args) {
        /*NLPManager.getInstance().getTrainer()
                .setCallback(new ITrainingCallback() {
                    @Override
                    public void corpusLoaded(int vocWords, int trainWords, int totalWords) {
                        System.out.println("corpusLoaded： 词表大小 = " + vocWords + " 训练词数 = " + trainWords + " 语料词数 = " + totalWords);
                    }

                    @Override
                    public void training(float alpha, float progress) {
                        System.out.println("training: alpha = " + alpha + " progress = " + progress);
                    }
                })
                .train("D:" + File.separator + "sogou_word2vec_training_set.txt", "D:" + File.separator + "training_out.bin");*/
    }
}
