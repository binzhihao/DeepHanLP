package test;

public class Demo3 {
    public static void main(String[] args) {
        /*try {
            AbstractVectorModel<String> model = NLPManager.getInstance().getWordVectorModel("D:" + File.separator + "training_out.bin");

            System.out.println(model.nearest("日本"));

        } catch (Throwable t) {
            LogUtil.logger.severe(t.toString());
        }*/
    }
}
